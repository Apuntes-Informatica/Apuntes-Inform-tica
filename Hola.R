# Esto se podr?a hacer tambi?n con un ciclo for
# Para que los alumnos lo vean de las dos formas 
m1 = c(3,4,5,6)
m2 = 3*m1
print(m2)
for (i in length(m1))
  m2[i] = 3 * m1[i]

#Esto te funciona porque m2 ya se ha creado previamente
#En la version que hemos hecho entre Loli y yo ya esta corregido
